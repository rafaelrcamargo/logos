[package]
edition = "2021"
name = "gatekeeper"
version = "0.1.0"

[dependencies]
actix-cors = "0.6.4"
actix-session = {version = "0.7.2", features = ["redis-actor-session"]}
actix-web = "4.3.0"
dotenv_codegen = "0.15.0"
utils = {path = "../utils"}
